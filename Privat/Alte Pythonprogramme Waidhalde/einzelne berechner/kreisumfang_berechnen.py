#kreisumfang berechnen

from math import*                               # alles aus math importieren
r = float (input("gib den radius ein: "))       # Variale r für radius
f = (r*2*pi)                                    # formel für pi
print ("der Umfang beträgt: ",f)                #Ausgabe Resultat
